DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/recallzero?schema=public"
AWS_REGION="us-east-1"
DYNAMODB_TABLE_NAME="recallzero-app"
# AWS credentials should come from your local AWS profile, environment variables,
# or the Vercel/AWS runtime role used during deployment.